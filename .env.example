# Motorsport Telemetry Analytics: environment configuration
# Copy to `.env` and adjust as needed. No secrets are required to run this project;
# FastF1 pulls public F1 timing/telemetry data and caches it locally.

# Where FastF1 stores its local cache of downloaded session data.
FASTF1_CACHE_DIR=data/cache
