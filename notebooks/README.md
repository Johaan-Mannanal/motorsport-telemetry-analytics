# Notebooks

Exploratory analysis lives here. The maintained, tested logic is in [`../src`](../src) and the
dashboard in [`../app.py`](../app.py); notebooks are for ad-hoc exploration and should import from
`src` rather than duplicating logic.
